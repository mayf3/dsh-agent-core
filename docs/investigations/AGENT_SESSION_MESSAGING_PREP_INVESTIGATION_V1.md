# AGENT_SESSION_MESSAGING_PREP_INVESTIGATION_V1

- status: investigation evidence (PREPARATION TRACK — grants no implementation or production authority)
- lane: AGENT_SESSION_MESSAGING (goal PRODUCTION_RND_PARALLEL_PREPARATION_V1)
- investigation base: github/main `840d2f4ad91f8252eb1f163330c041216a0dd9c4` (worktree branch `prep/session-messaging-investigation-v1`)
- primary specimen: local untracked draft `docs/specs/AGENT_CORE_AGENT_SESSION_MESSAGING_V1.md` (r2, status proposed, implementation_authority none) — read-only, lives only in the maintainer checkout, NOT on main
- method: static read of BASE code + draft specs; no test runs, no network, no installs
- companion evidence: `evidence/session-messaging-code-excerpts.md` (verbatim excerpts)

---

## A. CURRENT STATE AT BASE (840d2f4)

### A1. Broker capability pipeline

Manifest → tool registration:

1. Manifests are plain data validated by `validateManifest` (`packages/broker/src/schema.js:33-289`): id grammar (`schema.js:48-54`), `requiredScopes` (`schema.js:57-70`), `toolName` (`schema.js:72-79`), error table (`schema.js:101-120`), per-operation arguments (`schema.js:143-261`), and the LOCAL marker `local: true | { resource }` (`schema.js:122-141`, invariants `schema.js:263-271`: local capability must not declare http bindings).
2. `registerCapabilities` (`packages/broker/src/registry.js:199-201`) builds ONE defineTool per capability (`registry.js:66-173`): selector parameter (default `operation`, `registry.js:75-83`), merged parameter schema (`registry.js:85-99`), execute dispatches through `invoke` (`registry.js:158-164`).
3. `invoke` (`packages/broker/src/mapping.js:199-246`): `validateInvocation` (operation lookup + structural argument validation, `mapping.js:156-187`) → handler resolution → `resolvePrincipal` injected identity only (`mapping.js:209-211`) → envelope normalization; handler-thrown errors downgrade to a declared code (`mapping.js:216-219`); unknown codes fail closed (`resolveCode`, `mapping.js:146-153`). Structural validation supports bounds + declared `validationError` (`mapping.js:84-97`), string `minLength`/`nonBlank` (`mapping.js:99-106`), enum, `additionalProperties:false` (`mapping.js:78-82`), all-or-none groups (`mapping.js:125-132`). NOTE: no `maxLength`, no byte-length, no NUL check exist.
4. Default manifests (`packages/broker/src/index.js:84-92`): calculator, forum ×N, workflow, okr, agent-definition ×2, scheduler. NO agent_session_send manifest.

Execution modes:

5. CHILD mode (default, per-agent process, `index.js:286-320`): registers tools; every HTTP **and LOCAL** capability relays via parent-RPC — `const relays = hasHttp || manifest?.local !== undefined` (`index.js:304-309`) → `createRelayHandlers` (`index.js:309`). Relay forward is exactly `{ capabilityId, operation, args }` (`packages/broker/src/relay.js:161-165`); the gate `if (!op.http && !isLocalManifest) continue` with `isLocalManifest = manifest?.local !== undefined` (`relay.js:153-155`) means LOCAL operations DO get relay handlers at BASE. Relay unwraps the parent envelope back into invoke conventions (`relay.js:187-199`); transport failure → structured `{errorCode, detail}` (`relay.js:166-173`).
6. PARENT dispatch: `BROKER_RPC_METHOD = 'agent-core/broker'` (`relay.js:39`, `packages/agent-router/src/parent-rpc-relay.js:23`). The Router parent handler (`parent-rpc-relay.js:38-102`): ignores/logs child self-reported identity fields (`parent-rpc-relay.js:44-52`), derives caller from the actual `proc.agentId` spawning relationship (`parent-rpc-relay.js:81`), binds a trusted `ingressContext` ONLY when `active.callerAgentId === agentId && active.processGeneration === proc.processGeneration && rpcMeta.turnExecutionId === active.turnExecutionId && activeExecution !== undefined && activeExecution.settled !== true` (`parent-rpc-relay.js:53-63`), and answers inside the transport success envelope (`parent-rpc-relay.js:72-88`).
7. GATEWAY execute (`packages/broker/src/gateway.js:162-281`) order: capability lookup incl. local manifests (`gateway.js:76-81`); local handler resolution at EXECUTE time — static `localHandlers` wins, else `localHandlerResolver()` (`gateway.js:89-96`); missing handler → `unsupported_operation` fail-closed (`gateway.js:171-174`); frozen trusted context `callerAgentId = agentId` (`gateway.js:182`); **authoritative argument + trusted-context re-validation exists ONLY for `manifest.id === 'scheduler'`** (`gateway.js:186-207`); credential load fail-closed `credential_unavailable` (`gateway.js:209-221`); LOCAL capability grant check — `requiredScopes` + `local.resource` via auth-service `requestAccessToken`, denial → `access_denied` / `credential_invalid` / `transport_failure` BEFORE any handler (`gateway.js:225-253`; `requestAccessToken` at `packages/broker/src/transport.js:137`); handler invoked with frozen parent-owned context (`gateway.js:254-258`); handler throw → `internal_error` (scheduler mutations → `mutation_outcome_unknown`) (`gateway.js:259-270`).
8. localHandlerResolver injection: the control-plane composition passes `localHandlerResolver: () => ({ ...(ctx.get('agentDefinitionAccess')?.handlers ?? {}), ...(ctx.get('selfServiceSchedulerAccess')?.handlers ?? {}) })` (`packages/broker/src/index.js:272-275`). It is an execute-time **closed merge of exactly two named services** — a new provider (e.g. `agentSessionMessagingAccess`) requires editing this closure (small `broker/src/index.js` change) plus a `ctx.provide` in the composition (pattern: `packages/production-runtime/src/compose.js:433-450`).
9. Credentials/Principal/scope: child holds no credential (`index.js:22-35` header); the only principal source for HTTP-path handlers is the injected `resolvePrincipal` stub (`packages/broker/src/identity.js:33-47`); the gateway credential store read per call is `loadCredentialFor(credentialsFile, agentId)` (`gateway.js:138-139, 209-221`).
10. Audit at BASE: NO durable capability-audit store in broker/router. Precedents: scheduler self-service `appendAudit` with `auditStatus` appended/append_failed (`packages/scheduler/src/self-service.js:235, 318-325, 477`) wired with sanitized `onAuditFailure` (`compose.js:440-450`); in-memory bounded `auditBounded` in AgentProcess (`turn-execution.js:190`); route-chain journal (`route-chain.js:375`).

**agent_session_send zero-implementation: CONFIRMED.** Repo-wide grep over the worktree for `agent_session_send|agent-session-send|agentSessionSend|agent.session.send|agentSessionMessaging` across *.js/*.json/*.md hits ONLY `docs/decisions/AGENT_WORKSPACE_SESSION_MODEL_V3.md:384,981` (accepted D-008 doc references); **zero hits in any code file** (grep exit 1 on `--include=*.js`).

### A2. Session / Run model

- Delivery primitive: `agentRouter.deliver({ requestId, agentId, sessionMode: 'main'|'fresh', message })` (`packages/agent-router/src/ingress-delivery.js:173-286`). Validates 4 fields (`ingress-delivery.js:209-230`); REJECTS caller-supplied `sessionId` fail-loud (`ingress-delivery.js:221-226`); reconciliation capacity precheck before spawn/write (`ingress-delivery.js:235`); `sessionMode='main'` fixes `sessionId='main'` (`ingress-delivery.js:242-243`); `fresh` mints `fresh-<sha256(agentId\0requestId)>` durably inside the store mutation queue (`ingress-delivery.js:244-251`); workspace = agent default workspace (`ingress-delivery.js:257`); admission via `routeChain.admitWithRouteChain(agent.id, { sessionId, message, opts: { cwd, callerCorrelation: { requestId } } })` (`ingress-delivery.js:264-268`); returns `{ accepted: true, sessionId, reconciliationHandle?, evidence? }` (`ingress-delivery.js:279-285`).
- Run/Turn identity: per-execution handle minted by the Router reconciliation store BEFORE prompt bytes — `store.mintTurnExecution({ agentId, processGeneration, sessionId, callerCorrelation })` (`packages/agent-router/src/process/turn-execution.js:217-222`); parent protocol request id `promptRequestId = req-<processGeneration>-<seq>` (`turn-execution.js:236`); the handle doubles as `turnExecutionId` and travels to the child as `session/prompt` metadata (`turn-execution.js:319-333`) where AsyncLocalStorage preserves its origin for nested parent-RPC calls (`packages/demo-server/src/index.js:66-72, 90, 194-198`).
- Queue/busy: ALL prompt-producing paths share ONE bounded per-process FIFO (`turn-execution.js:142-155`, caps at `packages/agent-router/src/process/evidence-buffer.js:27-28`), one in-flight drain (`turn-execution.js:179-195`); deliver (receipt-only) resolves its caller immediately after the `session/prompt` receipt (`turn-execution.js:279-292`) and keeps background fence tracking (`turn-execution.js:290, 370-377`).
- Session create/resume: demo-server seam `getOrCreateSession` — live handle reuse (`packages/demo-server/src/session-seam.js:79-85`), concurrent first creation single-flighted via `pendingCreations` (`session-seam.js:86-91, 153-157`), persisted → resume (`session-seam.js:123-141`), absent → create with frozen `meta:{cwd}` (`session-seam.js:142-146`); prompt = `createUserMessage({ content, source: { kind: 'user' } })` then `followup(message)` returning `messageId` (`session-seam.js:162-167`).
- Identity ownership today: `requestId` — caller-supplied (Delivery V0 admission correlation; `fresh` session mint key; `callerCorrelation` index); `sessionId` — Router-owned ('main' fixed / fresh minted; caller input rejected); reconciliation handle / `turnExecutionId` — Router minted per Run; `promptRequestId` — AgentProcess minted; `messageId` — demo-server minted per accepted prompt; **no `sessionKey` concept exists**; provenance `source` — hardcoded `{kind:'user'}` (no inter_agent kind anywhere).
- Reconciliation seams on the router service: `getTurnReconciliation` / `readFinalAssistantOutput` / `resolveCallerCorrelation` / `onTurnReconciled` / `turnExecutionSnapshot` (`packages/agent-router/src/index.js:338-348`). Output states `available{text,truncated,originalBytes,terminalState} | pending | no_output{terminalState} | evicted | restart_lost | never_existed` (`packages/agent-router/src/reconciliation/query.js:95-110`); subscriber with disposer (`query.js:150-153`); terminal outcome vocabularies `DIRECT_OUTCOMES = completed|failed|not_admitted`, `LATE_OUTCOMES = late_completed|late_failed|terminated_without_outcome` (`packages/agent-router/src/reconciliation/state-machine.js:13-15`).

### A3. Infrastructure gaps a new LOCAL capability would hit

(a) **relay.js local-capability support: PRESENT at BASE.** `relay.js:153-155` gates on `!op.http && !isLocalManifest`, so a local manifest's operations get child-side relay handlers; the WAKE-spec-era gap (`relay.js:64 if (!op.http) continue` at baseline 1a9b81d, per local draft `docs/specs/AGENT_CORE_AGENT_WAKE_CAPABILITY_V1.md:124`) is fixed at 840d2f4. Caveat: `packages/broker/test/relay.test.js` (128 lines) has NO local-manifest coverage (only the http fixture at `relay.test.js:36`) — the fixed path is untested.
(b) **localHandlerResolver: generalized but CLOSED.** Not hard-bound to a single service anymore (`index.js:272-275` merges two), but a third provider name cannot be picked up without editing this closure. Gateway contract itself needs zero changes (`gateway.js:51-61, 89-96`).
(c) Other surfaces checked:
- manifest/schema validation for local capabilities: fully supported (`schema.js:122-141, 263-271`); `requiredScopes` optional for local read-style caps, grant-checked when declared (`gateway.js:226-253`).
- registry registration: no local-specific gap (`registry.js:199-201`).
- authoritative gateway-side argument re-validation: scheduler-only special case (`gateway.js:186-207`); any new local capability must self-validate in its trusted handler (or the gateway branch must be generalized).
- audit surface: none generic (see A1.10) — L0/L1 two-phase audit would be new code following the scheduler precedent.
- inbox/receipt semantics: real inbox receipt = `session/prompt` RPC response carrying `messageId` (`turn-execution.js:334-336`); queue admission alone never resolves a caller; queue-cap/fence rejections are pre-write `not_admitted` (`turn-execution.js:145-151, 169-176`).
- waiter primitives for timeoutSeconds: only the four reconciliation seams + `onTurnReconciled` exist; **no bounded Promise wait helper** anywhere (WAIT_REPLY_GAP real). Turn deadlines exist separately (`turn-execution.js:344-367` → `outcome_unknown`), independent of any caller reply-wait bound.

---

## B. SPEC-vs-CODE AUDIT — AGENT_CORE_AGENT_SESSION_MESSAGING_V1 r2 vs BASE 840d2f4

Spec investigation baseline is `origin/main@1a9b81d`; BASE here is newer (840d2f4). All verdicts are against BASE.

| # | Sev | Spec § | Code fact (BASE file:line) | Verdict |
|---|-----|--------|----------------------------|---------|
| F1 | note | §3.1 deliver flow | `ingress-delivery.js:208-286` validate→resolveAgentRef→capacity precheck (235)→sessionId 'main' (242-243)→workspace (257)→`admitWithRouteChain` (264-268)→receipt; `turn-execution.js:205-341`; `session-seam.js:77-167` | CONFIRMED |
| F2 | note | §3.1 main reuse semantics | live reuse `session-seam.js:79-85`; resume `123-141`; create `142-146`; concurrent creation single-flight `86-91,153-157`; process startup single-flight CAS `process-registry.js:87-139` | CONFIRMED |
| F3 | note | §3.1 two sends → two Runs, no requestId dedupe on main | sessionId independent of requestId `ingress-delivery.js:242-243`; each admission mints its own handle `turn-execution.js:217-222`; no dedupe path exists for 'main' | CONFIRMED |
| F4 | note | §3.2 busy = shared bounded FIFO, no steering | one shared queue + caps `turn-execution.js:142-155`; one in-flight `179-195`; receipt-only early resolve `279-292`; queue ownership via `queueReleasePromise` (291); test DLV2 `packages/agent-router/test/process-delivery.test.js:67-90` | CONFIRMED |
| F5 | note | §3.3 reconciliation seams + output states | router service `index.js:338-348`; states `reconciliation/query.js:95-110` | CONFIRMED |
| F6 | note | §3.3 no bounded wait helper (WAIT_REPLY_GAP) | only `get/read/onTurnReconciled/turnExecutionSnapshot` exist (`index.js:338-348`); no Promise-combining helper in `reconciliation/` | CONFIRMED |
| F7 | note | §3.4 provenance lost, source hardcoded user | `session-seam.js:164` `source: { kind: 'user' }`; `session/prompt` payload has no source field `turn-execution.js:319-333`; `deliver()` accepts only 4 fields `ingress-delivery.js:209-230` | CONFIRMED |
| F8 | note | §4 R1 "relay.js is not an implementation gap" | `relay.js:153-155` local manifests relayed; child routing `index.js:304-309`. WAKE-spec blockers (relay.js:64 / index.js:182 at 1a9b81d) are RESOLVED at BASE. Caveat: zero local-path test coverage (`relay.test.js:36` http-only) → census G8 | CONFIRMED (with test caveat) |
| F9 | minor | §4 R1 / §8 MOD list | resolver is a CLOSED two-service merge `index.js:272-275`; adding `agentSessionMessagingAccess` requires editing `packages/broker/src/index.js`, which §8's predicted scope does not list (§8 lists broker manifest+tests, production-runtime wiring) | CONFIRMED (spec MOD-list omission) |
| F10 | major | §2.2 decision-alignment gate | Gate text targets accepted V2 §11 (`AGENT_TO_AGENT_SESSION_SCOPE = PER_TASK`, worktree `docs/decisions/AGENT_WORKSPACE_SESSION_MODEL_V2.md:328-338`). At BASE, D-008 `docs/decisions/AGENT_WORKSPACE_SESSION_MODEL_V3.md` was ACCEPTED (2026-09-01, commit b2e3eb1, "整份取代 D-006 / V2", frontmatter lines 2-10) and freezes `AGENT_TO_AGENT_MESSAGING_SESSION_SCOPE = TARGET_MAIN`, one send = one new Run/Turn (V3:366-391), naming `agent_session_send` (V3:384-388), deferring implementation to §29 (V3:979-991) and listing the r2 spec as "read-only proposed input… 不在本轮接受或实现" (V3:19-21). The required whole-authority alignment HAS since been executed; spec §2.2/`related_decisions`(V2) and review-gate item 1 are stale | CONFIRMED STALE (spec must be amended docs-only before acceptance) |
| F11 | note | §4 R2 timeoutSeconds 0..300 expressible | integer + minimum/maximum + declared validationError supported `mapping.js:84-97`, `schema.js:314,321-333` | CONFIRMED |
| F12 | minor | §4 R2 message 1..65536 UTF-8 bytes, NUL forbidden | broker structural validator supports only `minLength` (UTF-16 units) + `nonBlank` for strings; NO `maxLength`, no byte-length, no NUL check (`mapping.js:99-106`, `schema.js:297-385`). Consistent with spec R2's mandate that the trusted handler performs authoritative first-action validation; child schema can only be defense-in-depth (partial) | CONFIRMED (handler-side enforcement mandatory) |
| F13 | note | §4 R2 "direct parent-RPC must not bypass authoritative validation" | gateway re-validates args + trusted context ONLY for scheduler (`gateway.js:186-207`); other local caps get no gateway-side re-validation → the handler must be the authority (as R2 assigns). Census G7 | CONFIRMED (mechanism is capability-special-cased) |
| F14 | note | §4 R3/R5 target-owned execution identity | child self-reported identity ignored `parent-rpc-relay.js:44-52`; caller = actual proc.agentId `:81`; `callerAgentId` frozen `gateway.js:182`; credential by actual agentId `gateway.js:209-221`; grant by auth-service only `gateway.js:225-253` | CONFIRMED |
| F15 | note | §4 R3 exact source-turn proof | binding requires generation match + `rpcMeta.turnExecutionId === active.turnExecutionId` + unsettled execution `parent-rpc-relay.js:53-63`; child supplies rpcMeta via AsyncLocalStorage `demo-server/src/index.js:66-72,90`. BUT `activeIngressContext` is installed ONLY for `mode === 'turn'` (`turn-execution.js:255-271`) → a source Run that itself originated via deliver() has NO bound context today; R3's requirement ("cannot depend on activeIngressContext being present") is a genuine router extension | CONFIRMED (gap real; spec §8 lists it) |
| F16 | note | §4 R3 self-send deadlock rationale | one in-flight per process; queued entries drain only after terminal `turn-execution.js:179-195` → self-send would queue behind its own Run | CONFIRMED |
| F17 | note | §4 R7/R10 timeout=0 real-receipt; queue receipt ≠ inbox receipt | caller resolves only after `promptWrite` returns with `receipt.messageId` (`turn-execution.js:276-292, 334-336`); caps/fence rejections are pre-write `not_admitted` (`145-151, 169-176`) | CONFIRMED |
| F18 | note | §4 R8 outcome inputs | truncation flag captured on bounded tail `turn-execution.js:84-103`; terminal vocab `state-machine.js:13-15`; output states incl. truncated + terminalState `query.js:95-110` | CONFIRMED |
| F19 | minor | §4 R8 closed mapping | `terminated_without_outcome` (LATE_OUTCOMES, `state-machine.js:13`) has NO row in the R8 table; a settled Run with that late outcome and null output surfaces as `{state:'no_output', terminalState:'terminated_without_outcome'}` matching neither the completed nor failed branch | CONFIRMED (mapping gap) |
| F20 | note | §4 R9 subscriber + disposer exist | `query.js:150-153` returns disposer; listener Set `reconciliation/store.js:60` — R9 algorithm implementable as written | CONFIRMED |
| F21 | note | §4 R11 external-route isolation | `deliver()` has no binding/ingress context and rejects caller sessionId (`ingress-delivery.js:221-226, 264-268`); feishu.reply only on feishu-entry onIngress path (`ingress-delivery.js:139-144`) — a deliver-based send structurally cannot touch Bindings or external routes | CONFIRMED |
| F22 | minor | §4 R12 L0/L1 audit | no capability audit append surface exists at BASE (A1.10); scheduler precedent `self-service.js:235,318-325` + sanitized failure signal `compose.js:440-450` shows the two-phase pattern is buildable; §8 correctly predicts "exact grant/error/audit mapping" in production-runtime | CONFIRMED (new surface required) |
| F23 | minor | §5 error taxonomy | gateway local-grant path can emit `transport_failure` (`gateway.js:246-248`) and missing-handler gives `unsupported_operation` (`gateway.js:171-174`); neither is in the spec §5 required error list; an undeclared code would fail closed to `invalid_arguments` (`mapping.js:146-153`), mislabeling a grant-check outage | CONFIRMED (taxonomy omission) |
| F24 | note | §8/§9 "local relay fix + execute-time resolver already in main" | `relay.js:153-155`; `index.js:272-275`; `gateway.js:89-96` | CONFIRMED at BASE |

Additional coverage checks (all CONFIRMED): no automatic replay (deliver is one-shot admission; rejections propagate — `ingress-delivery.js:264-286`); no automatic ping-pong (no auto B→A delivery exists anywhere; only feishu-entry replies); no arbitrary sessionId/sessionKey input (`ingress-delivery.js:221-226`; fresh ids minted from hash `244-251`; no sessionKey concept in repo); manifest identity `agent_session_send` / scope `agent.session.send` / resource `agent-session-messaging` all satisfy manifest grammar (`schema.js:50-51, 63-64, 74, 132-137`); 65536 < MAX_PROMPT_BYTES 1 MiB (`evidence-buffer.js:29`) matching §4 R2 rationale.

### Spec-readiness verdict

**REVISE — 0 blockers, 1 major (F10), 5 minor (F9, F12, F19, F22, F23), 18 notes.**

The contract design is implementable on BASE without new subsystems; every infrastructure claim the r2 spec makes about current main held up (including the two WAKE-era gaps it declares fixed). Revision is docs-only: (1) rewrite §2.2 + frontmatter `related_decisions` to record that accepted D-008/V3 executed the required whole-authority alignment and freezes TARGET_MAIN messaging; (2) add an R8 row for `terminated_without_outcome`; (3) add the `broker/src/index.js` resolver extension to §8's predicted scope; (4) add `transport_failure`/`unsupported_operation` handling to §5; (5) state explicitly that byte-length/NUL validation lives in the trusted handler (broker schema cannot express it).

---

## C. AUTHORITY DETERMINATION

- Draft spec frontmatter: `status: proposed`, `implementation_authority: none`, `production_apply_authority: none` (draft lines 3-7).
- The spec is NOT tracked on github/main: `git ls-files docs/specs/` at BASE 840d2f4 contains neither `AGENT_CORE_AGENT_SESSION_MESSAGING_V1.md` nor `AGENT_CORE_AGENT_WAKE_CAPABILITY_V1.md` (both are local untracked files in the maintainer checkout). Accepted D-008 (V3:19-21) likewise records the r2 spec as read-only proposed input, "不在本轮接受或实现".
- Under the repository governance (`AGENTS.md` → `.agents/README.md`: Investigation = evidence authority, grants no implementation permission; no accepted Spec → do not implement), the consequences for this preparation round are:
  - investigation + draft-spec work: ALLOWED (this document and the round report are evidence artifacts under `docs/investigations/` / `docs/reports/`);
  - candidate implementation of `agent_session_send`: **PAUSED_AUTHORITY — MUST NOT be started this round**, regardless of the REVISE outcome above, until (i) the spec is revised + independently accepted, and (ii) an implementation-authority grant exists. D-008 §29 explicitly defers `agent_session_send implementation` to future boundaries.

## D. GAP CENSUS — BASE → what the spec requires

| # | Label | Gap | Blocks final E2E? |
|---|-------|-----|-------------------|
| G1 | infrastructure | Provenance sidecar absent: no `messageOrigin` plumb-through in `deliver()` (`ingress-delivery.js:208-268`), `session/prompt` payload (`turn-execution.js:319-333`), or message source (`session-seam.js:164` hardcodes `kind:'user'`) | YES — durable `inter_agent` provenance is a core acceptance (Case G) |
| G2 | infrastructure | Race-safe bounded wait helper absent (`index.js:338-348` seams only; no waiter anywhere) | YES — timeoutSeconds>0 / Cases D + race scenarios |
| G3 | infrastructure | No `agentSessionMessagingAccess` handler provider/wiring (`index.js:272-275` closed two-service merge; provider pattern `compose.js:433-450`) | YES — capability fails closed `unsupported_operation` without it |
| G4 | infrastructure | No `agent_session_send` manifest in `DEFAULT_MANIFESTS` (`index.js:84-92`) — tool never registered | YES |
| G5 | infrastructure | Source-turn correlation for deliver-originated source Runs: `activeIngressContext` turn-mode-only (`turn-execution.js:255-271`) → `boundIngressContext` undefined for deliver sources (`parent-rpc-relay.js:53-63`); R3 demands this path work | Only for the A2A-onward-send acceptance case; not for a turn/Feishu-originated source |
| G6 | infrastructure | L0/L1 audit append surface absent (precedent only: `self-service.js:235`; `compose.js:440-450`) | YES per spec R12 (intent append gates delivery) |
| G7 | infrastructure | Gateway authoritative re-validation is scheduler-only (`gateway.js:186-207`); new capability must self-validate in its trusted handler (spec-conformant) or gateway branch generalized | NO (handler-side validation satisfies R2 as written) |
| G8 | test | relay local path has zero test coverage at BASE (`relay.test.js:36` http-only) | NO (code present), but spec §8 inventory/contract tests must add it |
| G9 | spec | R8 mapping missing `terminated_without_outcome` row (`state-machine.js:13`) | NO for code; must fix before spec acceptance |
| G10 | spec | §2.2 decision-gate + `related_decisions` stale vs accepted D-008/V3 (`docs/decisions/AGENT_WORKSPACE_SESSION_MODEL_V3.md:2-10, 366-391`) | YES for spec acceptance (docs-only fix); NO for code baseline |
| G11 | spec | §8 MOD list omits `packages/broker/src/index.js` resolver extension (F9) | NO; review-accuracy fix |
| G12 | authority | `implementation_authority: none`; spec absent from main; D-008 defers implementation | YES — by design, until a separate authority round |
| G13 | test | Acceptance Cases A–I + mandatory cases have zero coverage (feature absent) | YES — inherently, until implementation round |
| G14 | spec | §5 taxonomy omits `transport_failure` / `unsupported_operation` emitted by the existing grant/handler paths (`gateway.js:171-174, 246-248`) | NO; declare/mapping fix in spec + manifest error table |

Summary: 7 infrastructure, 3 spec, 1 authority, 3 test. Truly E2E-blocking today: G1, G2, G3, G4, G6 (+G5 for the onward-send case), all contingent on G12 authority by design.
