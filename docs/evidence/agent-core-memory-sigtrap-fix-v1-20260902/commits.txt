c21c8f3 SIGTRAP_FIX_V1 revise r2 (audit follow-ups, all non-blocking recommendations adopted): write-permits/read-refuses symmetry — esc() bounds the STORED (escaped) field length and tagsToText bounds the joined tags line so a legal write can never produce a value a later read would refuse; renderEntries document bound now includes id/updatedAt (x2 margin) and true scaffolding overhead (320/entry + 5/tagCount) making it a genuinely strict upper bound; migration tool on PREIMAGE_GATE failure now leaves the live file untouched (never clobbers a concurrent writer's newer content with the analyzed preimage) and exits 2, and failure-path reports go through writeReport; chmod/chown moved before the fsync so synced metadata is final; codec-contract comment scoped to the well-formed entry space; new guard tests for stored-bound/tags-line/document-bound; verified: 83/83 tests pass, revised code produces byte-identical migration output on the real ceo corpus (sha bd3a866b...)
d4802a9 AGENT_MEMORY_SIGTRAP_FIX_V1: repair source codec asymmetry (esc-on-render / no-decode-on-parse) that amplified every read-modify-write cycle ~2x per write until V8's replacement builder died SIGTRAP; freeze the source codec contract (stored = esc(v), parse = strict inverse) making parse(render(e))==e and render(parse(render(e))) a byte fixpoint; add MEMORY_GUARD bounded validation BEFORE every String.replace (field 8192 / content 65536 / tags 64 / file+render 32MiB) failing loud but non-fatal via MemoryGuardError with no partial writes (writeEntries freezes oversized files, carries mode/uid/gid, fsync file+dir, cleans tmp on failure); add recoverSource/isEscapeCanonical/escapeFieldValue for mechanically provable esc^k recovery (maximal unwrap, pair-free candidate unambiguous, bound/pair cases flagged not guessed); add one-off migration tool scripts/agent-memory-sigtrap-migration-v1.mjs rewriting ONLY amplified - **Source**: lines (dry-run default, immutable sha-verified backup 0400, preimage gate, same-dir tmp+fsync+rename+dir-fsync, post-validate parse-under-production-limits + render fixpoint, exact restore on failure); tests: codec contract + 100x memory/disk stability zero-growth, guard call-site non-fatality (consolidation daily-note fallback, injection degrade, file frozen), calibrated pathological child-process regression (old codec SIGTRAPs at 2^26-char source region, new code survives with structured refusal), migration CLI e2e
