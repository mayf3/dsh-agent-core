# MF1 affected conformance — nonproduction only

Basef8a713589659d4b2ec73f97e53b232968d35a6c4; authority/mandate and exact external
matrix are pinned in the companion preflight. Implementation scope is the fixed
one-shot HR stage and internal producer closure validation. Production apply=HOLD,
sourceClosureProven=false, bootstrap_capable=false, H2/H3/H4 not claimed.

stageHrOneShotApplication reuses stageApplication isolation/frozen commit overlay.
It validates both fixed ai.agent-core.runtime route byte projections before the
first stage write. Only ProgramArguments' existing fixed app Runtime script target
changes to the gated entry. All other route bytes, including UID/root/environment,
KeepAlive, WorkingDirectory and service topology, survive byte-identically. Unknown,
duplicate or unsupported target rejects before stage creation. No installed plist
is read/written. The staged gated entry and child helper come from candidateSHA,
never uncommitted bytes. The ordinary staged script becomes an immediate denial;
repository ordinary developer startup remains unchanged. Returned route projections
are preparation bytes for later exact packaging, not an installed route readback.

A closed fixed manifest binds the gated entry/helper hashes, retired-script denial
hash and two exact route targets. Assembled DS builder pins entry/helper source hashes.
The internal producer validates exact manifest and observed inhibition set (both
routes plus manual gated entry) before deriving sourceClosureComplete; it no longer
hard-codes True there. No request/receipt fields, generic callbacks or caller-selected
paths were added. Existing offline build_authorization remains an offline shaper;
manifest origin and continuous observation need the real guarded adapter. Fixtures
are explicitly synthetic and cannot manufacture production trust.

Executed RED: absent fixed stage function2/2 failed; missing manifest validator
failed. GREEN: new/old stage4/4, profile6/6 and assembled orchestration9/9 including
missing source zero authorization/no child/UNKNOWN custody. Both executable manifest
entry and retired script without descriptors reject before a throw-on-import Router
fixture. Existing candidate66/66 and real adapter9/9 run for affected builder closure.

Residual LE1: actual installed executable/entry inventory/permissions and continuous
zero same-UID actor/source exclusion through the one operation are UNKNOWN. No live
probe occurred. Missing real source inhibition/owned stop/holder derivation/live
window observation/child-V9-Router wiring/external bounded custody and real DS action
connection remain technical obligations as recorded in the preceding real-adapter
checkpoint. Technical MF1 PASS is not a live gate, a bootstrap package or recovery.
Independent affected review is parent-routed at the exact frozen successor.
