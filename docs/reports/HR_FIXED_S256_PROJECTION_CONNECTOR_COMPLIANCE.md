# Original projector connector — affected evidence

Base437a131eb1f144409c0113f140efc5af6733f78b; same accepted R2/r4/V2, mandate and
preflight. Existing actual JS projector nine-field output failed assembled source
at the snake_case hash access (RED PRECHECK_REJECTED/OBSERVATION_UNAVAILABLE).
Minimum connector validates the exact existing closed representation/identity,
passes complete session/times/preimage output to preflight unchanged, and derives
only internal authorization tuple/hash for existing journal APIs. No fallback schema,
new receipt/store field or weakened validation. Synthetic fixture now uses real
camelCase projector schema and actual auth epoch for its bundle retirement.

Actual JS pending-store projector→assembled DS handler→original immutable Python
intent/authorization hash/epoch join GREEN; Node4/4 including prior H4 real-module
join. Assembled10/10 includes private extra, wrong handle alias, Boolean generation
and invalid hash: zero effect/zero intent, PRECHECK_REJECTED. Source/store bytes of
actual Node fixture remain unchanged; only disposable Python operation fixture runs.
All OS observations/activation remain synthetic; actual fixed_io and real production
entry remain inactive. No host proof, H2-H6 closure or bootstrap readiness claimed.
Remaining source inhibition/owned stop/live continuity, installed holder derivation,
actual child V9/Router context, real action connection and bounded external custody
are still absent implementation, not merely final run permission. Parent routes
changed-surface review; production and LE1 observations remain UNKNOWN/forbidden.
