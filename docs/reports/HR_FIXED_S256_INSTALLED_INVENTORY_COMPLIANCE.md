# Fixed source/holder observation — partial nonproduction

Base75024addb213f0c880aa6c2babccdc828ce04f45; same accepted fixed R2/r4/V2
and sole-writer mandate in the companion DEVELOPMENT_PREFLIGHT. Real method source
is embedded in the disconnected DS candidate; activation remains unset, and real
production action remains PROFILE_NOT_BOOTSTRAPPED before protected reads/effects.

Implemented: fixed root-owned MF1 manifest; no-follow gated/helper/retired installed
entry byte pins; both fixed plist label/ProgramArguments/root projections and rejection
of entry/root overrides. Fixed binding/primary metadata is read under owned descriptors
and duplicate/unknown payload keys reject. Actual installed BindingStore,
workspace-bootstrap path functions and DSH encode/project functions derive paths.
All matching exact session-header candidates across bounded project directories are
included, along with binding workspaces, primary import/default and the full agent
sessions root. Only first header line is read, byte-by-byte through newline; no private
session body or credential file is read or archived. Unknown headers fail closed.
Missing optional primary import retains the existing default rule with bound/rechecked
absence. Missing bindings or session metadata rejects rather than fabricating closure.

Descriptor-relative no-follow directory opens and retained file/directory descriptors
bind device/inode/owner/mode/size/timestamps and input bytes; fresh path identity and
input/header bytes are rechecked before return. Directory enumeration, child stdin/
stdout and output cap are bounded while streaming. Existing fixed projector preflight
budget is preserved; remaining metadata calls share an internal absolute10s deadline.
Accepted collector10s/challenge750ms/operation300s remain distinct, unchanged. No
bounded timeout grants release, launch or retry. Returned descriptor identities are
observations, never serialized process/OFD custody or sustained window ownership.

RED: adapter absent; actual disposable Python-FD→Node join failed until its real
transitive validator dependency/FD positioning was included; directory replacement
during derivation initially escaped detection. Minimum fix binds derived directories
before subsequent observations and rechecks them. GREEN: inventory9/9 (loops include
missing/changed/escaped/unknown/private metadata, races, bounded enumeration/deadline/
stream cap), actual primary+binding+historical-header candidate derivation, assembled
activation zero-IO. Real adapter10/10 and orchestration10/10 also pass. No live methods
were invoked: tests patch root/UID/process boundaries and use disposable source bytes.

The result never emits sourceClosureComplete: known MF1 entries are a partial installed
observation, and unresolvedSources retains LE1_INSTALLED_ENTRY_AND_RESUMPTION_CLOSURE.
holderCoverage explicitly names installed config and exact headers observed; this does
not infer absent legacy/historical cwd coverage or exclude an unlisted host source.
Full trusted finite source/holder closure must be fresh and complete in the operation;
unknown/ambiguous coverage remains ineligible. Point UID0 count cannot upgrade it.

Still missing real methods: complete actual source inventory/inhibition and owned-stop/
continuous window observations; authentic pinned child with ongoing V9 responder and
Router context; bounded UNKNOWN custodian; actual fixed_io/action connection. Their
source implementation remains authorized nonproduction work. Installed binaries,
complete source/holder facts, actual mutex topology, rollback/floor/readback facts and
final exact activation/run authority remain UNKNOWN/unexercised. No new durable
store/receipt fields, generic path/action API, production readiness or H2-H6 claim.
