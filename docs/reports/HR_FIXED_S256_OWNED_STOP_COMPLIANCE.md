# Private owned-stop connector — confined nonproduction checkpoint

Accepted R2/r4 reused. Source and tests are isolated; production entry remains PROFILE_NOT_BOOTSTRAPPED. Original unauthorized bootout exception remains host-effect/rc/walltime UNKNOWN in sealed exception artifact; this checkpoint never resolves it or claims compensation.

Actual fixed handler now registers a private duplicate of the canonical mutation-lock open-file description. Immediately after existing durable intent/new exclusive window it captures private canonical/window duplicates, binds exact immutable intent digest, rejects same-inode separately opened canonical capability, wrong/reused/lost descriptors, changed fixed paths, observed unlocked descriptors and stale intent. Checks occur at existing boundaries; UNKNOWN retains private owner together with existing custody. Close only closes duplicates, never performs LOCK_UN. No durable field added. The finite stop object accepts this private owner; actual guards/incomplete inventory still reject before any command.

Permanent original missing-stopOwner RED retained. Confined RED found guard failure could recover to positive; now observed guard loss permanently poisons owner. Confined RED found second-open canonical inode could bind; private handler registration now binds exact OFD, not only inode/flock contention. All capability checks are observations at boundaries; unobserved lock gaps, complete launch-source continuity or LE1 are not inferred.

Final executed evidence: owner12/12 (canonical+window release, closed/reused/wrong OFD, stale intent, inactive guard, partial inventory, shared30s fixed-route synthetic command/no replay), fake confinement3/3, filtered actual handler retention1/1 with zero process dispatch attempts. Mandatory shared Popen hard denial starts before any assembly/import/setup and covers all teardown; no original Popen fallback. Budget test replaces the exact closure cell with a pure recorder and retains outer denial. No actual subprocess/launchctl/host execution in these runs. Existing broad action/child suites were not rerun because strict confinement forbids their subprocess path.

Remaining implementation: current production source/holder inventory reports LE1 unresolved; complete continuous source/window coverage is absent. Real fixed IO/control-flow activation, actual observed controlled-stop receipt producer, actual child V9/Router/startup/readback integration, external bounded UNKNOWN custody/handoff and verified release remain incomplete. This connector is not H2-H6, child_real_exit, complete sourceClosureComplete, installed/bootstrap-ready package or incident recovery proof. Same reviewer must inspect exact final affected delta including previously interrupted WIP; no self-issued independent PASS.
