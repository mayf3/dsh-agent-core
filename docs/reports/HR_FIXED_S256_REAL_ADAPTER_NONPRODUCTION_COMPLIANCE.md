# Guarded fixed real-adapter increment — partial/nonproduction

Authority and mandate are pinned in the companion DEVELOPMENT_PREFLIGHT. Base is
73c40c24e961bcaaadae4b2732e282698ffd9fd8. Conformance is affected-method only;
production_ready=false, source_closure_proven=false, bootstrap_capable=false.

Implemented source methods: bounded no-follow/digest/duplicate-key protected
receipt input; exact floor/validator timestamps and existing DS admitted-capture
read-only verification with check_live=True; fixed installed Node/UID inherited-FD
settlement projection via pinned validators; original immutable commitment subject
binding; stable store/validator identity readback; bounded child output/deadline;
O_EXCL/fsync/directory-fsync/exact-readback root evidence receipt. Builder embeds
these guarded methods. Unset exact activation digest rejects before any protected
read, even when effective UID is root. Request grammar and inactive real action are
unchanged. No protected input or root execution occurred in tests.

Executed: real adapter Python9/9; directly dependent orchestration8/8; existing
candidate Python65/65; actual disposable-store Node readback2/2. Initial missing
adapter/activation-contract RED and receipt namespace AttributeError RED preceded
minimum fixes. Node tests invoke the actual fixed consumer and durable store under
nonroot synthetic trust, then validate settled and pre-settlement projections and
zero store writes. Python OS/DS boundaries are mocked or disposable, not host proof.

Still absent implementation: trusted installed holder-path derivation; finite
applicable source inhibition and owned stop/live window methods; continuous source
and holder observation; actual pinned child startup adapter and ongoing V9 JSON
responder/Router context wiring; complete real action integration; externally
bounded UNKNOWN custody/handoff. The actual projector's camelCase schema connector
also remains to be integrated. These are technical implementation obligations,
not hidden final Owner gates. Implemented read-only methods still require exact
protected floor/validator/rollback facts and reviewed activation/run package, which
remain UNKNOWN and have never been inspected. Existing private process-local custody
is not external or death-safe custody proof. Next queued accepted nonproduction work
is MF1 staged closed entry for both routes; its tests cannot prove live LE1 closure.

No claim of full H2/H3/H4, installed lock ownership, HR recovery, or independent PASS.
Parent routes exact-head affected review. Frozen historical packages and accepted
Specs remain byte-identical; no production mutation or bootstrap is authorized.
