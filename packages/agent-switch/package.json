{
  "name": "@agent-core/agent-switch",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Core DSH switch adapter: registers the agent_core.switch_agent tool inside a per-agent process; forwards the request over the demo-server parent-RPC relay to the Router's switchAgent domain operation. Owns zero policy (no binding persistence, no agent lookup, no session selection, no entry branching, no history)",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./package.json": "./package.json"
  },
  "license": "UNLICENSED",
  "peerDependencies": {
    "@deepseek-ai/cordis": ">=3",
    "@deepseek-ai/dsh-tools": ">=0",
    "@deepseek-ai/schemastery": ">=0"
  }
}
