{
  "name": "@agent-core/scheduler-router",
  "version": "0.0.0",
  "private": true,
  "description": "Agent Core Scheduler ↔ Router final integration bridge — real invokeAgent over the existing Router domain surface + deliver adapter over the existing Feishu outbound seam",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "license": "UNLICENSED"
}
