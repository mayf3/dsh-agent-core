name: PR140 Governance Adoption Final-Head Evidence (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow revision is retained only as tooling history."
          echo "Use pr140-governance-adoption-final-head-evidence-v2.yml."
